[phases.setup]
nixPkgs = ["python311", "gcc", "tesseract", "tesseract-ocr-eng", "tesseract-ocr-por"]

[phases.install]
cmds = ["python -m venv /opt/venv && /opt/venv/bin/pip install -r requirements.txt"]

[start]
cmd = "/opt/venv/bin/python bot.py"